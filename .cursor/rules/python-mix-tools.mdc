---
description: Python 混合小工具开发最佳实践（文档/Excel/数据处理）
globs: **/*.py
alwaysApply: false
---

# Python Mix Tools 最佳实践

## 1. 语言与风格

- 使用 Python 3.9+，优先使用现代语法（如 `typing`、`pathlib`、`dataclasses`、结构模式匹配等）
- 优先函数式风格：纯函数、不可变优先、避免全局可变状态
- 使用类型注解，便于维护与 IDE 支持
- 代码包含关键逻辑的清晰注释，避免冗余

## 2. 文档处理

- 使用 `pathlib.Path` 处理路径，避免字符串拼接
- 读取/写入统一使用 `encoding='utf-8'`，显式指定编码
- 大文件采用流式或分块读取，避免一次性加载到内存
- 敏感操作前校验路径存在性、权限和文件类型

## 3. Excel 处理

- 大表优先用 `openpyxl` 的 `read_only` / `write_only` 模式，或 `pandas` 按块读取
- 批量读写时用 `iter_rows` / `DataFrame` 批量操作，避免逐格读写
- 列名、表头统一用常量或配置定义，便于扩展

## 4. 数据解析

- 明确数据源格式（JSON/CSV/XML 等），使用对应标准库或成熟库
- 对空值、异常行、编码错误做显式处理，不静默忽略
- 数据量大时考虑生成器、分块处理，减少内存占用

## 5. 错误与边界

- 捕获具体异常类型，记录上下文后向上抛出或转换为业务异常
- 校验空值、空列表、空文件等边界情况
- 对用户输入和外部数据做校验与清洗

## 6. 扩展性

- 功能按职责拆分到独立模块，便于后续新增子功能
- 公共逻辑抽象为工具函数或类，复用优先
- 配置与硬编码分离，支持通过配置或参数调整行为
